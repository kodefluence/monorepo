package command

// Config carry all of command config in a single struct
type Config struct {
	// Default to "monorepo"
	Name string

	// Default to "A placeholder you need to actually change it"
	Short string
}
